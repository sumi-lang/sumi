[package]
name = "sumi-cli"
version = "0.1.0"
edition = "2024"
license.workspace = true

[[bin]]
name = "sumi"
path = "src/main.rs"

[dependencies]
sumi-format = { path = "../format" }
sumi-frontend = { path = "../frontend" }
sumi-hir = { path = "../hir" }
sumi-lexer = { path = "../lexer" }
sumi-syntax = { path = "../syntax" }
sumi-text = { path = "../text" }

[dev-dependencies]
tempfile.workspace = true
