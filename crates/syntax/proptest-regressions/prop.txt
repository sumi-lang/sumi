# Seeds for failure cases proptest has generated in the past. It is
# automatically read and these particular cases re-run before any
# novel cases are generated.
#
# It is recommended to check this file in to source control so that
# everyone who runs the test benefits from these saved cases.
cc c79afe96b393fb26355cfd9f199cb10f00c7d5fddd8157ae2e54cc2eccde6739 # shrinks to an `=` inserted before the `->` of a signature, found by CI on the first push of #60
cc 9c921b00cfa07454c8bc4bee42b84c811e01722568ccb53980699b9e7eb68846 # shrinks to an item's `fn` swapped with the token before it, which leaves its headless signature behind garbage on its line
cc c37db1d157ca28e9f58f6b9b657708d0e35af7b24a0a1305bcd2554c436c8ea4 # shrinks to (source, index, edit) = ("fn foo() = false\n\nfn foo(foo: int,) {\n(false) = if fn(a: int) -> int { a } { foo } // c\n{ true\n  >= if foo {\nreturn a // c\n} else {\nb = 0 // c\n_ = b // c\nx1 = fn() -> int {\n\"s\" = \"s\" // c\nlet mut b: int = fn(foo: int) = true // c\n} - 0 / !x1 || if false {\n0 = true\nlet x1: int = foo\n}\n  * (1000) - {\nlet mut x1: int = b // c\nfalse = true\nreturn 1000\n} <= for x1 in x1..a {\nlet b = x1 // c\n42 // c\n} * a\n}\n  / b\n  - {\nx1 = !for foo in 0..1000 { x1 } % !{ foo }\n  >= a\n  + {\n_ = b\nfalse = x1 // c\ntrue\n} && 42 <= b\n  % for a in 0..b {\n_ = x1\nfoo = false\n}\n  + !if foo {} else if 1000 { foo }\n  % foo\n} % -0 }\n  % true + \"s\"\n  * a\n  && a / -x1 = {}\n}\n", 19, Duplicate)
