//! The grammar's coverage witnesses: for every node kind, one per child
//! its rule allows, as [`Witness`] describes. A body of trees covers the
//! grammar when every witness is present on some node of its kind, and
//! absent on some node when the rule makes the child optional.
//!
//! @generated by `cargo xtask codegen` from `sumi.grammar`; do not edit.

use sumi_syntax::ast::{
    ArgList, AssignStmt, AstNode, BinaryExpr, Block, CallExpr, ClosureExpr, DiscardStmt,
    ElseBranch, FnItem, IfExpr, LetStmt, Param, ParamList, ParenExpr, PrefixExpr, ReturnStmt,
    SourceFile,
};
use sumi_syntax::{BinaryOp, NodeKind, SyntaxKind};

use crate::coverage::{Witness, has_operator, has_tokens};

/// Every witness, in grammar order.
pub const WITNESSES: &[Witness] = &[
    Witness {
        parent: NodeKind::SourceFile,
        name: "items: FnItem",
        optional: true,
        present: |_, tree, node| {
            let view = SourceFile::cast(tree, node).expect("a node of the parent kind");
            view.items(tree).next().is_some()
        },
    },
    Witness {
        parent: NodeKind::FnItem,
        name: "name: Name",
        optional: false,
        present: |_, tree, node| {
            let view = FnItem::cast(tree, node).expect("a node of the parent kind");
            view.name(tree).is_some()
        },
    },
    Witness {
        parent: NodeKind::FnItem,
        name: "param_list: ParamList",
        optional: false,
        present: |_, tree, node| {
            let view = FnItem::cast(tree, node).expect("a node of the parent kind");
            view.param_list(tree).is_some()
        },
    },
    Witness {
        parent: NodeKind::FnItem,
        name: "ret: TypeRef",
        optional: true,
        present: |_, tree, node| {
            let view = FnItem::cast(tree, node).expect("a node of the parent kind");
            view.ret(tree).is_some()
        },
    },
    Witness {
        parent: NodeKind::FnItem,
        name: "body: Expr",
        optional: false,
        present: |_, tree, node| {
            let view = FnItem::cast(tree, node).expect("a node of the parent kind");
            view.body(tree).is_some()
        },
    },
    Witness {
        parent: NodeKind::FnItem,
        name: "'fn'",
        optional: false,
        present: |lexed, tree, node| has_tokens(lexed, tree, node, &[SyntaxKind::FnKw]),
    },
    Witness {
        parent: NodeKind::FnItem,
        name: "'->'",
        optional: true,
        present: |lexed, tree, node| {
            has_tokens(lexed, tree, node, &[SyntaxKind::Minus, SyntaxKind::Gt])
        },
    },
    Witness {
        parent: NodeKind::FnItem,
        name: "'='",
        optional: true,
        present: |lexed, tree, node| has_tokens(lexed, tree, node, &[SyntaxKind::Eq]),
    },
    Witness {
        parent: NodeKind::ParamList,
        name: "params: Param",
        optional: true,
        present: |_, tree, node| {
            let view = ParamList::cast(tree, node).expect("a node of the parent kind");
            view.params(tree).next().is_some()
        },
    },
    Witness {
        parent: NodeKind::ParamList,
        name: "'('",
        optional: false,
        present: |lexed, tree, node| has_tokens(lexed, tree, node, &[SyntaxKind::LParen]),
    },
    Witness {
        parent: NodeKind::ParamList,
        name: "','",
        optional: true,
        present: |lexed, tree, node| has_tokens(lexed, tree, node, &[SyntaxKind::Comma]),
    },
    Witness {
        parent: NodeKind::ParamList,
        name: "')'",
        optional: false,
        present: |lexed, tree, node| has_tokens(lexed, tree, node, &[SyntaxKind::RParen]),
    },
    Witness {
        parent: NodeKind::Param,
        name: "name: Name",
        optional: false,
        present: |_, tree, node| {
            let view = Param::cast(tree, node).expect("a node of the parent kind");
            view.name(tree).is_some()
        },
    },
    Witness {
        parent: NodeKind::Param,
        name: "type_ref: TypeRef",
        optional: true,
        present: |_, tree, node| {
            let view = Param::cast(tree, node).expect("a node of the parent kind");
            view.type_ref(tree).is_some()
        },
    },
    Witness {
        parent: NodeKind::Param,
        name: "':'",
        optional: true,
        present: |lexed, tree, node| has_tokens(lexed, tree, node, &[SyntaxKind::Colon]),
    },
    Witness {
        parent: NodeKind::Name,
        name: "Ident",
        optional: false,
        present: |lexed, tree, node| has_tokens(lexed, tree, node, &[SyntaxKind::Ident]),
    },
    Witness {
        parent: NodeKind::TypeRef,
        name: "Ident",
        optional: false,
        present: |lexed, tree, node| has_tokens(lexed, tree, node, &[SyntaxKind::Ident]),
    },
    Witness {
        parent: NodeKind::Block,
        name: "stmts: Stmt",
        optional: true,
        present: |_, tree, node| {
            let view = Block::cast(tree, node).expect("a node of the parent kind");
            view.stmts(tree).next().is_some()
        },
    },
    Witness {
        parent: NodeKind::Block,
        name: "'{'",
        optional: false,
        present: |lexed, tree, node| has_tokens(lexed, tree, node, &[SyntaxKind::LBrace]),
    },
    Witness {
        parent: NodeKind::Block,
        name: "'}'",
        optional: false,
        present: |lexed, tree, node| has_tokens(lexed, tree, node, &[SyntaxKind::RBrace]),
    },
    Witness {
        parent: NodeKind::LetStmt,
        name: "name: Name",
        optional: false,
        present: |_, tree, node| {
            let view = LetStmt::cast(tree, node).expect("a node of the parent kind");
            view.name(tree).is_some()
        },
    },
    Witness {
        parent: NodeKind::LetStmt,
        name: "type_ref: TypeRef",
        optional: true,
        present: |_, tree, node| {
            let view = LetStmt::cast(tree, node).expect("a node of the parent kind");
            view.type_ref(tree).is_some()
        },
    },
    Witness {
        parent: NodeKind::LetStmt,
        name: "initializer: Expr",
        optional: false,
        present: |_, tree, node| {
            let view = LetStmt::cast(tree, node).expect("a node of the parent kind");
            view.initializer(tree).is_some()
        },
    },
    Witness {
        parent: NodeKind::LetStmt,
        name: "'let'",
        optional: false,
        present: |lexed, tree, node| has_tokens(lexed, tree, node, &[SyntaxKind::LetKw]),
    },
    Witness {
        parent: NodeKind::LetStmt,
        name: "'mut'",
        optional: true,
        present: |lexed, tree, node| has_tokens(lexed, tree, node, &[SyntaxKind::MutKw]),
    },
    Witness {
        parent: NodeKind::LetStmt,
        name: "':'",
        optional: true,
        present: |lexed, tree, node| has_tokens(lexed, tree, node, &[SyntaxKind::Colon]),
    },
    Witness {
        parent: NodeKind::LetStmt,
        name: "'='",
        optional: false,
        present: |lexed, tree, node| has_tokens(lexed, tree, node, &[SyntaxKind::Eq]),
    },
    Witness {
        parent: NodeKind::AssignStmt,
        name: "target: Expr",
        optional: false,
        present: |_, tree, node| {
            let view = AssignStmt::cast(tree, node).expect("a node of the parent kind");
            view.target(tree).is_some()
        },
    },
    Witness {
        parent: NodeKind::AssignStmt,
        name: "value: Expr",
        optional: false,
        present: |_, tree, node| {
            let view = AssignStmt::cast(tree, node).expect("a node of the parent kind");
            view.value(tree).is_some()
        },
    },
    Witness {
        parent: NodeKind::AssignStmt,
        name: "'='",
        optional: false,
        present: |lexed, tree, node| has_tokens(lexed, tree, node, &[SyntaxKind::Eq]),
    },
    Witness {
        parent: NodeKind::DiscardStmt,
        name: "value: Expr",
        optional: false,
        present: |_, tree, node| {
            let view = DiscardStmt::cast(tree, node).expect("a node of the parent kind");
            view.value(tree).is_some()
        },
    },
    Witness {
        parent: NodeKind::DiscardStmt,
        name: "'_'",
        optional: false,
        present: |lexed, tree, node| has_tokens(lexed, tree, node, &[SyntaxKind::Underscore]),
    },
    Witness {
        parent: NodeKind::DiscardStmt,
        name: "'='",
        optional: false,
        present: |lexed, tree, node| has_tokens(lexed, tree, node, &[SyntaxKind::Eq]),
    },
    Witness {
        parent: NodeKind::ReturnStmt,
        name: "value: Expr",
        optional: true,
        present: |_, tree, node| {
            let view = ReturnStmt::cast(tree, node).expect("a node of the parent kind");
            view.value(tree).is_some()
        },
    },
    Witness {
        parent: NodeKind::ReturnStmt,
        name: "'return'",
        optional: false,
        present: |lexed, tree, node| has_tokens(lexed, tree, node, &[SyntaxKind::ReturnKw]),
    },
    Witness {
        parent: NodeKind::NameRef,
        name: "Ident",
        optional: false,
        present: |lexed, tree, node| has_tokens(lexed, tree, node, &[SyntaxKind::Ident]),
    },
    Witness {
        parent: NodeKind::LiteralExpr,
        name: "IntLiteral",
        optional: false,
        present: |lexed, tree, node| has_tokens(lexed, tree, node, &[SyntaxKind::IntLiteral]),
    },
    Witness {
        parent: NodeKind::LiteralExpr,
        name: "StringLiteral",
        optional: false,
        present: |lexed, tree, node| has_tokens(lexed, tree, node, &[SyntaxKind::StringLiteral]),
    },
    Witness {
        parent: NodeKind::LiteralExpr,
        name: "'true'",
        optional: false,
        present: |lexed, tree, node| has_tokens(lexed, tree, node, &[SyntaxKind::TrueKw]),
    },
    Witness {
        parent: NodeKind::LiteralExpr,
        name: "'false'",
        optional: false,
        present: |lexed, tree, node| has_tokens(lexed, tree, node, &[SyntaxKind::FalseKw]),
    },
    Witness {
        parent: NodeKind::PrefixExpr,
        name: "operand: Expr",
        optional: false,
        present: |_, tree, node| {
            let view = PrefixExpr::cast(tree, node).expect("a node of the parent kind");
            view.operand(tree).is_some()
        },
    },
    Witness {
        parent: NodeKind::PrefixExpr,
        name: "'-'",
        optional: false,
        present: |lexed, tree, node| has_tokens(lexed, tree, node, &[SyntaxKind::Minus]),
    },
    Witness {
        parent: NodeKind::PrefixExpr,
        name: "'!'",
        optional: false,
        present: |lexed, tree, node| has_tokens(lexed, tree, node, &[SyntaxKind::Bang]),
    },
    Witness {
        parent: NodeKind::BinaryExpr,
        name: "lhs: Expr",
        optional: false,
        present: |_, tree, node| {
            let view = BinaryExpr::cast(tree, node).expect("a node of the parent kind");
            view.lhs(tree).is_some()
        },
    },
    Witness {
        parent: NodeKind::BinaryExpr,
        name: "rhs: Expr",
        optional: false,
        present: |_, tree, node| {
            let view = BinaryExpr::cast(tree, node).expect("a node of the parent kind");
            view.rhs(tree).is_some()
        },
    },
    Witness {
        parent: NodeKind::BinaryExpr,
        name: "'||'",
        optional: false,
        present: |lexed, tree, node| has_operator(lexed, tree, node, BinaryOp::Or),
    },
    Witness {
        parent: NodeKind::BinaryExpr,
        name: "'&&'",
        optional: false,
        present: |lexed, tree, node| has_operator(lexed, tree, node, BinaryOp::And),
    },
    Witness {
        parent: NodeKind::BinaryExpr,
        name: "'=='",
        optional: false,
        present: |lexed, tree, node| has_operator(lexed, tree, node, BinaryOp::Eq),
    },
    Witness {
        parent: NodeKind::BinaryExpr,
        name: "'!='",
        optional: false,
        present: |lexed, tree, node| has_operator(lexed, tree, node, BinaryOp::Ne),
    },
    Witness {
        parent: NodeKind::BinaryExpr,
        name: "'<'",
        optional: false,
        present: |lexed, tree, node| has_operator(lexed, tree, node, BinaryOp::Lt),
    },
    Witness {
        parent: NodeKind::BinaryExpr,
        name: "'<='",
        optional: false,
        present: |lexed, tree, node| has_operator(lexed, tree, node, BinaryOp::Le),
    },
    Witness {
        parent: NodeKind::BinaryExpr,
        name: "'>'",
        optional: false,
        present: |lexed, tree, node| has_operator(lexed, tree, node, BinaryOp::Gt),
    },
    Witness {
        parent: NodeKind::BinaryExpr,
        name: "'>='",
        optional: false,
        present: |lexed, tree, node| has_operator(lexed, tree, node, BinaryOp::Ge),
    },
    Witness {
        parent: NodeKind::BinaryExpr,
        name: "'+'",
        optional: false,
        present: |lexed, tree, node| has_operator(lexed, tree, node, BinaryOp::Add),
    },
    Witness {
        parent: NodeKind::BinaryExpr,
        name: "'-'",
        optional: false,
        present: |lexed, tree, node| has_operator(lexed, tree, node, BinaryOp::Sub),
    },
    Witness {
        parent: NodeKind::BinaryExpr,
        name: "'*'",
        optional: false,
        present: |lexed, tree, node| has_operator(lexed, tree, node, BinaryOp::Mul),
    },
    Witness {
        parent: NodeKind::BinaryExpr,
        name: "'/'",
        optional: false,
        present: |lexed, tree, node| has_operator(lexed, tree, node, BinaryOp::Div),
    },
    Witness {
        parent: NodeKind::BinaryExpr,
        name: "'%'",
        optional: false,
        present: |lexed, tree, node| has_operator(lexed, tree, node, BinaryOp::Rem),
    },
    Witness {
        parent: NodeKind::ParenExpr,
        name: "inner: Expr",
        optional: false,
        present: |_, tree, node| {
            let view = ParenExpr::cast(tree, node).expect("a node of the parent kind");
            view.inner(tree).is_some()
        },
    },
    Witness {
        parent: NodeKind::ParenExpr,
        name: "'('",
        optional: false,
        present: |lexed, tree, node| has_tokens(lexed, tree, node, &[SyntaxKind::LParen]),
    },
    Witness {
        parent: NodeKind::ParenExpr,
        name: "')'",
        optional: false,
        present: |lexed, tree, node| has_tokens(lexed, tree, node, &[SyntaxKind::RParen]),
    },
    Witness {
        parent: NodeKind::CallExpr,
        name: "callee: Expr",
        optional: false,
        present: |_, tree, node| {
            let view = CallExpr::cast(tree, node).expect("a node of the parent kind");
            view.callee(tree).is_some()
        },
    },
    Witness {
        parent: NodeKind::CallExpr,
        name: "arg_list: ArgList",
        optional: false,
        present: |_, tree, node| {
            let view = CallExpr::cast(tree, node).expect("a node of the parent kind");
            view.arg_list(tree).is_some()
        },
    },
    Witness {
        parent: NodeKind::ArgList,
        name: "args: Expr",
        optional: true,
        present: |_, tree, node| {
            let view = ArgList::cast(tree, node).expect("a node of the parent kind");
            view.args(tree).next().is_some()
        },
    },
    Witness {
        parent: NodeKind::ArgList,
        name: "'('",
        optional: false,
        present: |lexed, tree, node| has_tokens(lexed, tree, node, &[SyntaxKind::LParen]),
    },
    Witness {
        parent: NodeKind::ArgList,
        name: "','",
        optional: true,
        present: |lexed, tree, node| has_tokens(lexed, tree, node, &[SyntaxKind::Comma]),
    },
    Witness {
        parent: NodeKind::ArgList,
        name: "')'",
        optional: false,
        present: |lexed, tree, node| has_tokens(lexed, tree, node, &[SyntaxKind::RParen]),
    },
    Witness {
        parent: NodeKind::IfExpr,
        name: "condition: Expr",
        optional: false,
        present: |_, tree, node| {
            let view = IfExpr::cast(tree, node).expect("a node of the parent kind");
            view.condition(tree).is_some()
        },
    },
    Witness {
        parent: NodeKind::IfExpr,
        name: "then_branch: Block",
        optional: false,
        present: |_, tree, node| {
            let view = IfExpr::cast(tree, node).expect("a node of the parent kind");
            view.then_branch(tree).is_some()
        },
    },
    Witness {
        parent: NodeKind::IfExpr,
        name: "else_branch: IfExpr",
        optional: true,
        present: |_, tree, node| {
            let view = IfExpr::cast(tree, node).expect("a node of the parent kind");
            matches!(view.else_branch(tree), Some(ElseBranch::IfExpr(_)))
        },
    },
    Witness {
        parent: NodeKind::IfExpr,
        name: "else_branch: Block",
        optional: true,
        present: |_, tree, node| {
            let view = IfExpr::cast(tree, node).expect("a node of the parent kind");
            matches!(view.else_branch(tree), Some(ElseBranch::Block(_)))
        },
    },
    Witness {
        parent: NodeKind::IfExpr,
        name: "'if'",
        optional: false,
        present: |lexed, tree, node| has_tokens(lexed, tree, node, &[SyntaxKind::IfKw]),
    },
    Witness {
        parent: NodeKind::IfExpr,
        name: "'else'",
        optional: true,
        present: |lexed, tree, node| has_tokens(lexed, tree, node, &[SyntaxKind::ElseKw]),
    },
    Witness {
        parent: NodeKind::ClosureExpr,
        name: "param_list: ParamList",
        optional: false,
        present: |_, tree, node| {
            let view = ClosureExpr::cast(tree, node).expect("a node of the parent kind");
            view.param_list(tree).is_some()
        },
    },
    Witness {
        parent: NodeKind::ClosureExpr,
        name: "ret: TypeRef",
        optional: true,
        present: |_, tree, node| {
            let view = ClosureExpr::cast(tree, node).expect("a node of the parent kind");
            view.ret(tree).is_some()
        },
    },
    Witness {
        parent: NodeKind::ClosureExpr,
        name: "body: Expr",
        optional: false,
        present: |_, tree, node| {
            let view = ClosureExpr::cast(tree, node).expect("a node of the parent kind");
            view.body(tree).is_some()
        },
    },
    Witness {
        parent: NodeKind::ClosureExpr,
        name: "'fn'",
        optional: false,
        present: |lexed, tree, node| has_tokens(lexed, tree, node, &[SyntaxKind::FnKw]),
    },
    Witness {
        parent: NodeKind::ClosureExpr,
        name: "'->'",
        optional: true,
        present: |lexed, tree, node| {
            has_tokens(lexed, tree, node, &[SyntaxKind::Minus, SyntaxKind::Gt])
        },
    },
    Witness {
        parent: NodeKind::ClosureExpr,
        name: "'='",
        optional: true,
        present: |lexed, tree, node| has_tokens(lexed, tree, node, &[SyntaxKind::Eq]),
    },
];
