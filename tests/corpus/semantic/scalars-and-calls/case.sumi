fn answer() -> int = (twice)(21)
fn twice(x: int) -> int {
    let y: int = x * 2
    y
}
fn cafe(e: bool) -> bool = !e
fn ignore(x: unit) -> unit {
    _ = answer()
    x
}
fn sub(a: int, b: int) -> int = a - b
fn ordered() -> int = sub(8, 3) - 2 * 1 - 4
